#include<iostream>

class Processos {
    int base;
    int limite;
    unsigned int PID;
    int* p;
    int* t;
    int indice;

    // Implementar comparacao entre Processos, i. e.,
    // Processo A > B, <=> A.t[ind] > B.t[ind]
}
